{
  "name": "vera",
  "version": "0.1.0",
  "private": true,
  "description": "Verifiable Execution & Registry Architecture",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "conformance": "pnpm dlx tsx scripts/conformance.ts"
  },
  "keywords": [
    "verifiable",
    "execution",
    "registry",
    "ledger",
    "deterministic",
    "audit"
  ],
  "author": "",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "vitest": "3.2.4"
  }
}